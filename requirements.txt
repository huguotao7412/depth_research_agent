# ==========================================
# DepthResearch-Agent 核心依赖清单
# ==========================================

# 1. Web & API 服务框架 (基石层)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.3
aiofiles>=23.2.1          # 修复补全：用于 Kimi 解析器的全异步文件 I/O
requests>=2.31.0          # 修复补全：前端 Streamlit 与后端 API 通信

# 2. UI 交互体验层 (前端层)
streamlit>=1.32.0

# 3. 大模型与多智能体生态 (大脑层)
langchain>=0.1.13
langchain-core>=0.1.33
langchain-openai>=0.1.1
langchain-community>=0.0.29
langchain-text-splitters>=0.0.1
langgraph>=0.0.26         # 提供 Checkpointer 记忆持久化机制

# 4. 外部工具与全网检索 (外延层)
langchain-mcp-adapters>=0.1.0  # 核心补充：用于挂载 MCP (Tavily/GitHub)

# 5. RAG 知识库与高效检索引擎 (存储层)
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
tiktoken>=0.6.0

# 6. Ragas 量化评测套件 (评测层 - 修复补全)
ragas>=0.1.4
datasets>=2.18.0
pandas>=2.2.0

# 7. 网络通信与环境变量 (基建层)
httpx>=0.27.0
python-dotenv>=1.0.1